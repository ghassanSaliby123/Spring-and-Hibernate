Spring boot with spring data
using the jpaRepository to provide the CRUD operation 
no need to implement the DAO, we just inject the jpaRepository in the service layer
Spring Data ---Uses---> JPA spec ----Uses---> Hibernate 
